<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:local="clr-namespace:LogGrokCore"
                    xmlns:localControls="clr-namespace:LogGrokCore.Controls"
                    xmlns:mahapps="http://metro.mahapps.com/winfx/xaml/controls"
                    mc:Ignorable="d">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/styles/controls.xaml" />
        <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/styles/themes/light.blue.xaml" />
        <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/styles/controls.contextmenu.xaml" />

        <ResourceDictionary Source="ListViewItemStyle.xaml" />
    </ResourceDictionary.MergedDictionaries>
    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    <Style x:Key="FilterContextMenuItem"
           TargetType="MenuItem"
           BasedOn="{StaticResource MahApps.Styles.MenuItem}"
           d:DataContext="{d:DesignInstance Type=local:LogViewModel, IsDesignTimeCreatable=False}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=ItemsSource}"
                         Value="{x:Null}">
                <DataTrigger.Setters>
                    <Setter Property="Visibility" Value="Collapsed" />
                </DataTrigger.Setters>
            </DataTrigger>
            <DataTrigger Binding="{Binding CanFilter, Converter={StaticResource BooleanToVisibilityConverter}}"
                         Value="False">
                <DataTrigger.Setters>
                    <Setter Property="Visibility" Value="Collapsed" />
                </DataTrigger.Setters>
            </DataTrigger>
        </Style.Triggers>
    </Style>
    <DataTemplate x:Key="LogViewTemplate" DataType="local:LogViewModel">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <mahapps:MetroProgressBar
                Height="3"
                HorizontalAlignment="Stretch"
                Value="{Binding Progress}"
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <localControls:ListView Grid.Row="1" ItemsSource="{Binding Lines}"
                                    FontFamily="Consolas"
                                    SelectionMode="Extended"
                                    ReadonlySelectedItems="{Binding SelectedItems,
                                                            Mode=OneWayToSource}"
                                    View="{Binding CustomView}"
                                    ItemContainerStyle="{StaticResource ItemContainerStyle}"
                                    IsSynchronizedWithCurrentItem="True"
                                    ScrollViewer.VerticalScrollBarVisibility="Visible"
                                    ScrollViewer.CanContentScroll="True"
                                    VirtualizingStackPanel.IsVirtualizing="True"
                                    VirtualizingStackPanel.VirtualizationMode="Recycling"
                                    localControls:NavigateToLineBehavior.NavigateToLineRequest=
                                        "{Binding NavigateToLineRequest}">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <localControls:VirtualizingStackPanel
                            CurrentPosition="{Binding CurrentItemIndex}" 
                            localControls:HighlightedTextBlock.HighlightRegex="{Binding HighlightRegex}">
                            <localControls:VirtualizingStackPanel.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Command="Copy" />
                                    <MenuItem Header="Exclude" Style="{StaticResource FilterContextMenuItem}">
                                        <MenuItem.ItemsSource>
                                            <MultiBinding
                                                Converter="{localControls:MetaInfoToMenuItemsConverter}">
                                                <Binding Path="MetaInformation" />
                                                <Binding Path="ExcludeCommand" />
                                                <Binding Path="SelectedItems" />
                                            </MultiBinding>
                                        </MenuItem.ItemsSource>
                                    </MenuItem>
                                    <MenuItem Header="Exclude All But" Style="{StaticResource FilterContextMenuItem}">
                                        <MenuItem.ItemsSource>
                                            <MultiBinding
                                                Converter="{localControls:MetaInfoToMenuItemsConverter}">
                                                <Binding Path="MetaInformation" />
                                                <Binding Path="ExcludeAllButCommand" />
                                                <Binding Path="SelectedItems" />
                                            </MultiBinding>
                                        </MenuItem.ItemsSource>
                                    </MenuItem>
                                    <MenuItem Header="Clear filters"
                                              Visibility="{Binding HaveFilter, Converter={StaticResource BooleanToVisibilityConverter}}"
                                              Command="{Binding ClearExclusionsCommand}" />
                                </ContextMenu>
                            </localControls:VirtualizingStackPanel.ContextMenu>
                        </localControls:VirtualizingStackPanel>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
            </localControls:ListView>
        </Grid>
    </DataTemplate>
</ResourceDictionary>